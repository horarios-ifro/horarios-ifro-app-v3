import { Header } from "../Header/Header";

const PageClassesHeader = () => (
  <>
    <Header goBackTo={"/"} title="Turmas" containerProps={{ maxWidth: "sm" }} />
  </>
);

export default PageClassesHeader;
